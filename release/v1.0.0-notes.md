VulnEvidenceOps v1.0.0 freezes the public Python API, CLI, runtime-dependency and byte-exact JSON
Schema surfaces for the synthetic stable-reference boundary.

Independent human review was not performed. The repository owner explicitly waived that
prerequisite; the accountable waiver remains committed with `review_completed: false`.

This release does not attest production safety, vulnerability absence, scanner completeness,
remediation effectiveness or regulatory compliance. It does not publish a package to a package
index and does not deploy anything.
